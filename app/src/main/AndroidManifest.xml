<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kotlinnews">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".KotlinNewsApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.newsDetail.NewsDetailRxActivity"></activity>
        <activity android:name=".ui.activities.news.NewsRxActivity" />
        <activity android:name=".ui.activities.news.NewsActivity" />
        <activity
            android:name=".ui.activities.newsDetail.NewsDetailActivity"
            android:label="@string/title_activity_news_detail" />
        <activity android:name=".ui.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>